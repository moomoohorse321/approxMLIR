# Retired Side Paths

The old side-path probe scripts were removed from the runtime examples. They
covered direct-Triton microprobes and TorchInductor/HuggingFace TTIR dumping
that are no longer part of the current SGLang quantization mainline.

Use the mainline first:

- `../sglang_quant/probe_sglang_triton_dump.py`
- `../sglang_quant/sweep_sglang_quant.py`
